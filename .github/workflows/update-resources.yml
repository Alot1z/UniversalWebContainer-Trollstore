name: Update Resources (DISABLED)

# This workflow is disabled due to failures
# To enable: remove the comment below and fix the resource URLs
# on:
#   schedule:
#     - cron: '0 2 * * *'  # Daily at 2 AM UTC
#   workflow_dispatch:

on:
  workflow_dispatch:
    inputs:
      force_update:
        description: 'Force update resources'
        required: false
        default: false
        type: boolean

jobs:
  update-resources:
    runs-on: ubuntu-latest
    if: false  # Disabled
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Update resources
        run: |
          echo "Resource update workflow is currently disabled"
          echo "To enable: remove the 'if: false' condition"
