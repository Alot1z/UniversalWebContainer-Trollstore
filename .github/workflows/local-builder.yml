name: Local Builder Trigger (DISABLED)

# This workflow is completely disabled to save GitHub credits
# The LocalBuilder runs entirely on your local machine
# No need for GitHub Actions credits

# on:
#   workflow_dispatch:
#     inputs:
#       trigger_type:
#         description: 'Trigger type for LocalBuilder'
#         required: true
#         default: 'manual'
#         type: choice
#         options:
#         - manual
#         - scheduled
#         - api

on:
  workflow_dispatch:
    inputs:
      enable:
        description: 'Enable LocalBuilder trigger (uses GitHub credits)'
        required: true
        default: false
        type: boolean

jobs:
  trigger-local-builder:
    runs-on: ubuntu-latest
    if: false  # Always disabled to save credits
    steps:
      - name: LocalBuilder Disabled
        run: |
          echo "🚫 LocalBuilder Trigger is DISABLED"
          echo "💡 This saves GitHub Actions credits"
          echo "🔧 Use the START LOCAL BUILDER button in README instead"
          echo "💻 LocalBuilder runs entirely on your local machine"
