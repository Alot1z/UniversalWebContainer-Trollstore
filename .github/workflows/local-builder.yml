name: Local Builder Trigger

on:
  workflow_dispatch:
    inputs:
      trigger_type:
        description: 'Trigger type for LocalBuilder'
        required: true
        default: 'manual'
        type: choice
        options:
        - manual
        - scheduled
        - api

jobs:
  trigger-local-builder:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger LocalBuilder
        run: |
          echo "🚀 LocalBuilder Trigger Activated"
          echo "📅 Triggered at: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          echo "🔧 Trigger type: ${{ github.event.inputs.trigger_type }}"
          echo ""
          echo "📋 This workflow is a dummy trigger for the LocalBuilder system"
          echo "🔗 The actual LocalBuilder runs on your local machine"
          echo "💻 Use the START LOCAL BUILDER button in README.md"
          echo ""
          echo "✅ LocalBuilder trigger completed successfully"
